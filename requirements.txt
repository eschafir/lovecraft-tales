# Core Framework & UI
gradio==6.26.0
torch==2.13.0
torchaudio==2.11.0
torchcodec==0.16.0          # required by torchaudio.load() on torchaudio>=2.9; needs FFmpeg's shared libs inside this env, e.g. `conda install -c conda-forge ffmpeg=7`
transformers==4.51.3        # pinned: newer 5.x releases silently break CosyVoice3's text conditioning (produces garbled audio, no crash)
x-transformers==2.28.1

# Model Hub & Checkpoints
huggingface-hub==0.36.2     # held down by transformers==4.51.3's own pin; diffusers/gradio ask for newer but both still work fine against this version
modelscope==1.39.1

# Audio Processing & Feature Extraction
librosa==0.11.0
soundfile==0.14.0
pyworld==0.3.5
openai-whisper==20250625
setuptools<81               # pyworld imports pkg_resources at runtime, which setuptools>=81 removed

# CosyVoice / Matcha-TTS runtime deps
conformer==0.3.2
diffusers==0.40.0
lightning==2.6.5
unidecode==1.4.0
gdown==6.1.0
matplotlib==3.10.9
phonemizer==3.4.0
pyarrow==25.0.1
rootutils==1.0.7
wget==3.2
# piper_phonemize intentionally omitted: no macOS wheel exists, and it's not on CosyVoice3's actual
# import path (only pulled in by an unused Matcha-TTS CLI text-cleaner)

# Pipeline & Configuration
hydra-core==1.3.5
HyperPyYAML==1.2.3
omegaconf==2.3.1
inflect==7.5.0
wetext==0.1.6
networkx==3.4.2
numpy==1.26.4
pydantic==2.13.4
rich==15.0.0

# Runtime (CPU/CoreML for macOS)
onnx==1.22.0
onnxruntime==1.23.2
